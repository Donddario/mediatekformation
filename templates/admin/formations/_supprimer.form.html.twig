<form method="post" action="{{ path('admin.formations.delete', {'id': formation.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer la formation {{ formation.title }} ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>